// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id                          BigInt              @id @default(autoincrement())
  registeredAt                DateTime            @default(now())
  fullName                    String
  githubAccessToken           String
  receivedMaintenanceInvites  MaintenanceInvite[] @relation("Invitee")
  sentMaintenanceInvites      MaintenanceInvite[] @relation("FromMaintainer") 
  packages                    Package[]
}

model MaintenanceInvite {
  id                  BigInt          @id @default(autoincrement())
  invitee             User            @relation("Invitee", fields: [inviteeId], references: [id])
  inviteeId           BigInt
  fromMaintainer      User            @relation("FromMaintainer", fields: [fromMaintainerId], references: [id])
  fromMaintainerId    BigInt
}

// Packages are optional namespaces:
//
// - com.XXXXX
// - net.XXXXX
// - org.XXXXX
// - me.XXXXX
// - !{com,net,org,me} (for example, prisma is a package and prisma.util is a subpackage of prisma)
//
// Subnamespaces also work for each of the above examples, such as `com.microsoft.azure.util`.

model Package {
  id                  BigInt            @id @default(autoincrement())
  name                String
  maintainers         User[]
  versions            PackageVersion[]
}

model PackageVersion {
  id                  BigInt                   @id @default(autoincrement())
  versionNumber       String
  publishedAt         DateTime                 @default(now())
  zip                 Bytes
  package             Package                  @relation(fields: [packageId], references: [id])
  packageId           BigInt
  dependencies        PackageVersion[]         @relation("Dependency")
  dependents          PackageVersion[]         @relation("Dependency")
  yanked              Boolean                  @default(false)
}